---
title: "Hantera API-nycklar och utvecklarinställningar"
description: "Skapa och hantera API-nycklar, webhooks och loggar för integrationer mot sajns REST API"
---

På sidan [Utvecklare](https://app.sajn.se/~/settings/workspace/developer) finns allt som behövs för att bygga mot sajns REST API: API-nycklar, webhooks, övervakning och loggar. Den här guiden är en rundtur av flikarna. Den skapar ingen nyckel – API-nycklar är riktiga inloggningsuppgifter och ska bara skapas när du faktiskt behöver en.

Sidan hör till **arbetsytan**, inte organisationen. Nycklar och webhooks du skapar här gäller den arbetsyta du står i.

## Innan du börjar

- Du är inloggad och har behörighet att hantera API-nycklar eller webhooks i arbetsytan.
- API-åtkomst ingår i Team och Enterprise.

## Steg för steg

<Steps>
  <Step title="Öppna Inställningar och sedan Utvecklare">
    Fliken **Hem** samlar det du behöver för att komma igång: genvägar till dokumentationen, MCP-servern och sajns Postman-samling, adressen till MCP-servern, och arbetsytans **API-gränser**. Gränserna – förfrågningar per minut och per dygn – räknas per organisation, och `X-RateLimit-*`-headrarna i svaren visar aktuell status.

    Längst ned kan du prenumerera på **Utvecklarnyheter**, som går till din inloggningsadress.

    <Frame>
      <img src="/images/guider/settings-developer-api/02-open-developer.png" alt="Fliken Hem under Utvecklare med genvägar, MCP-adress och API-gränser" />
    </Frame>
  </Step>
  <Step title="Fliken API-nycklar listar arbetsytans nycklar">
    Tabellen på fliken [API-nycklar](https://app.sajn.se/~/settings/workspace/developer/api-keys) visar **Namn**, en maskad **API-nyckel** (klicka på den för att avslöja prefixet), när nyckeln **skapades**, när den **senast användes** och när den **utgår**. Sökfältet uppe till vänster filtrerar på namn.

    <Frame>
      <img src="/images/guider/settings-developer-api/03-open-api-keys.png" alt="Fliken API-nycklar med tabell och knappen Skapa API-nyckel" />
    </Frame>
  </Step>
  <Step title="Skapa API-nyckel">
    Dialogen har två fält: ett **namn** och hur länge nyckeln ska gälla – *30 dagar*, *90 dagar*, *1 år* eller *Aldrig*.

    Det finns ingen behörighetsväljare. Nyckeln **ärver dina behörigheter i arbetsytan**: ändras dina behörigheter, eller tas du bort från arbetsytan, slutar nyckeln fungera.

    När nyckeln genereras visas hela värdet **bara en gång**. Kopiera den direkt och spara den säkert – du kommer inte att kunna se den igen.

    <Frame>
      <img src="/images/guider/settings-developer-api/04-open-create-key.png" alt="Dialogen Skapa ny API-nyckel med namn och utgångstid" />
    </Frame>
  </Step>
  <Step title="Menyn på varje rad – regenerera eller återkalla">
    Menyn längst till höger på en nyckelrad låter dig:

    - **Regenerera** – byter ut nyckelns hemliga värde mot ett nytt, som visas en gång. Det gamla värdet slutar fungera direkt, så uppdatera dina integrationer.
    - **Återkalla** – tar bort nyckeln helt. Alla anrop med den avvisas omedelbart.

    Längst ned i menyn står vem som skapade nyckeln, så att du kan stämma av internt innan du gör något destruktivt.
  </Step>
  <Step title="Övervakning visar API- och webhook-hälsa">
    Fliken **Övervakning** sammanfattar de senaste 24 eller 72 timmarna: antal **API-förfrågningar**, **API-felfrekvens**, **svarstid p50 och p95**, antal **webhook-leveranser**, **webhook-fel** och **leveranstid p50 och p95**. Under nyckeltalen ligger två diagram – förfrågningar per timme och webhook-leveranstid.

    Statistiken bygger på loggarna och täcker därför samma 72 timmar som de.

    <Frame>
      <img src="/images/guider/settings-developer-api/09-monitoring.png" alt="Fliken Övervakning med nyckeltal och två diagram" />
    </Frame>
  </Step>
  <Step title="Loggar har både API-anrop och webhook-leveranser">
    Fliken **Loggar** innehåller två tabeller:

    - **API-loggar** – varje anrop mot REST-API:t, med tid, metod, sökväg, statuskod, varaktighet och IP-adress. Filtrera på metod eller statuskod, eller sök på sökväg och IP. Klicka på en rad för att se hela anropet.
    - **Webhook-loggar** – utgående anrop sajn skickat till era endpoints, med webhook, händelse, URL, status och svarskod.

    Båda sparas i **72 timmar** och är det första stället att titta när en integration inte beter sig som väntat.

    <Frame>
      <img src="/images/guider/settings-developer-api/13-logs.png" alt="Fliken Loggar med API-loggar överst och webhook-loggar under" />
    </Frame>
  </Step>
</Steps>

## De andra flikarna

- **OAuth-appar** – egna OAuth-klienter, för den som bygger en app som andra sajn-organisationer ska kunna ansluta.
- **Webhooks** – prenumerationer på händelser i arbetsytan. Se [Konfigurera webhooks](/guider/webhooks).
- **Sandbox** – en separat testmiljö kopplad till organisationen, så att du kan bygga utan att röra skarpa dokument.

Två flikar dyker bara upp när funktionen är påslagen för organisationen: **sajn Login** och **Inbäddning**.

<Warning>
  En API-nyckel ger åtkomst till arbetsytans data. Lägg den aldrig i klientkod, i ett publikt repo eller i en skärmdump. Misstänker du att en nyckel läckt – återkalla den direkt.
</Warning>
