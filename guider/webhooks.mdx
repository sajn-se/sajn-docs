---
title: "Konfigurera webhooks för dokumenthändelser"
description: "Ta emot HTTP-callbacks i realtid när dokument signeras, avböjs eller slutförs i sajn"
---

Med **webhooks** får din integration ett HTTP-anrop i realtid när något händer i sajn – när någon signerar, avböjer eller när hela dokumentet är klart. Den här guiden visar var webhooks ligger, vad dialogen innehåller och vilka händelser du kan prenumerera på. Guiden **skapar ingen** webhook – den stänger dialogen via *Avbryt*.

Webhooks hör till **arbetsytan** du står i. Vill du ha samma händelser från flera arbetsytor behöver du en webhook per arbetsyta.

## Innan du börjar

- Du är inloggad och har behörighet att hantera webhooks i arbetsytan.
- API-åtkomst ingår i Team och Enterprise.
- Din mottagande endpoint går att nå över HTTPS från publika internet. Adresser som pekar inåt i ett privat nät avvisas.

## Steg

<Steps>
  <Step title="Öppna Inställningar, Utvecklare och fliken Webhooks">
    Öppna [Inställningar → Utvecklare → Webhooks](https://app.sajn.se/~/settings/workspace/developer/webhooks). Tabellen visar **URL** (din mottagande endpoint), **Event** (vilka händelser som utlöser anropet) och **Skapad**. Sökfältet uppe till vänster filtrerar listan, och menyn längst till höger på varje rad har **Redigera** och **Ta bort**.

    Texten överst påminner om var leveranserna hamnar: under fliken **Loggar**.

    <Frame>
      <img src="/images/guider/webhooks/02-open-webhooks.png" alt="Fliken Webhooks med tom tabell och knappen Skapa webhook" />
    </Frame>
  </Step>
  <Step title="Dialogen Skapa webhook">
    Klicka **Skapa webhook**. Fälten är:

    - **Webhook URL** – den HTTPS-endpoint sajn ska skicka JSON till, t.ex. `https://dittforetag.se/api/webhook`. URL:en valideras innan webhooken sparas.
    - **Aktiv** – sätt **Nej** för att skapa webhooken pausad.
    - **Händelser** – en eller flera händelser som ska utlösa anropet. Se nästa steg.
    - **E-post vid fel (valfritt)** – får ett mejl om sajn inte lyckas leverera anropet.
    - **Hemlighet** – nyckeln som signerar varje anrop. Uppdateringsikonen i fältet fyller i ett nytt slumpat värde. Lämnar du fältet tomt skapar sajn en hemlighet åt dig.

    <Frame>
      <img src="/images/guider/webhooks/05-fill-url.png" alt="Dialogen Skapa webhook med URL, händelser, felmejl och hemlighet. Hemligheten är maskad" />
    </Frame>
  </Step>
  <Step title="Välj bland tillgängliga händelser">
    Händelsenamnen följer mönstret `<resurs>.<verb>`. Skriv i fältet för att filtrera listan.

    **Dokument** – `document.created`, `document.sent`, `document.signed`, `document.completed`, `document.rejected`, `document.expired`, `document.withdrawn`, `document.modified`, `document.deleted`, `document.restored`, `document.archived`, `document.unarchived`, `document.expiration.extended`, `document.archive.uploaded`.

    **Per part** – `document.party.sent`, `document.party.delivery.failed`, `document.party.opened`, `document.party.read`, `document.party.signed`, `document.party.rejected`, `document.party.delegated`, `document.party.auth.failed`, `document.party.added`, `document.party.removed`.

    **Påminnelser** – `document.reminder.automatic`, `document.reminder.manual`.

    **ID-kontroll** – `id.created`, `id.sent`, `id.opened`, `id.verified`, `id.failed`, `id.canceled`.

    **Kontakter och mallar** – `contact.created`, `contact.updated`, `contact.deleted`, `template.created`, `template.updated`, `template.deleted`, `template.restored`.

    **Organisation** – `workspace.created`, `member.added`, `login.completed`, `login.failed`.

    **Säkerhet** – `security.document.downloaded`, `security.documents.exported`, `security.signature.identity.accessed`, `security.workspace.member.removed`, `security.workspace.member.role.changed`, `security.workspace.role.updated`, `security.workspace.retention.updated`. De är tänkta att matas in i ett SIEM.

    Välj så få som möjligt – en endpoint som prenumererar på allt får snabbt hög trafik. För de flesta integrationer räcker `document.signed` och `document.completed`.

    <Frame>
      <img src="/images/guider/webhooks/06-open-events.png" alt="Listan över tillgängliga händelser i dialogen" />
    </Frame>
  </Step>
</Steps>

## Verifiera att anropet kommer från sajn

Varje leverans skickas med tre headers:

- `X-Sajn-Signature` – `t=<unix-sekunder>,v1=<hex>`, där `v1` är en HMAC-SHA256 över strängen `<t>.<rå body>` med din hemlighet som nyckel. Tidsstämpeln ingår i det signerade, så en tidigare leverans kan inte spelas upp igen. Avvisa anrop som är äldre än din tolerans.
- `X-Sajn-Delivery` – ett id som är detsamma i alla omförsök av samma händelse. Använd det för att inte göra samma arbete två gånger.
- `X-Sajn-Secret` – hemligheten i klartext. Den finns kvar för äldre integrationer och ska inte användas i nya. Jämför signaturen i stället.

## Rotera hemligheten

Öppna webhooken via menyn på raden och välj **Redigera**, klicka på uppdateringsikonen i fältet **Hemlighet** och spara. Den nya hemligheten används direkt för alla efterföljande anrop – uppdatera din verifieringskod innan du sparar, annars faller inkommande anrop på valideringen.

## När något inte kommer fram

Under fliken [Loggar](https://app.sajn.se/~/settings/workspace/developer/logs) finns **Webhook-loggar** med varje utskick: händelse, URL, status och svarskod. Loggarna sparas i 72 timmar. Fliken [Övervakning](https://app.sajn.se/~/settings/workspace/developer/monitoring) sammanfattar samma period som antal leveranser, felandel och leveranstid.

Misslyckas en leverans gör sajn flera omförsök. Har du fyllt i **E-post vid fel** får du ett mejl när alla försök är slut.

## Automatisk paus

En endpoint som fortsätter misslyckas pausas. Två villkor måste vara uppfyllda:

- Webhooken har **minst 5 misslyckade leveranser i rad**. En leverans räknas när dess omförsök är slut, inte per försök.
- Felserien har pågått tillräckligt länge: **1 timme** om varje fel i serien svarade `401`, `403`, `404` eller `410`, annars **72 timmar**, nätverksfel inräknade.

En enda lyckad leverans nollställer serien. En endpoint som svarar `2xx` på vissa händelser och misslyckas med andra når därför aldrig gränsen, hur länge felen än fortsätter.

En pausad webhook får inga anrop alls förrän du återaktiverar den. Raden visar då statusen **Automatiskt pausad** med orsak och tidpunkt, och du får en notis i appen och ett mejl till adressen i **E-post vid fel**.

Öppna menyn på raden och välj **Återaktivera utan test** för att starta om webhooken direkt. Felräknarna nollställs. Vill du testa mottagaren först skickar sajn om den senaste misslyckade leveransen – de sparas i 7 dagar – och återaktiverar webhooken bara om testet går fram.

## Nästa steg

<CardGroup cols={2}>
  <Card title="Utvecklarinställningar" icon="key" href="/guider/settings-developer-api">
    API-nycklar, övervakning och loggar på samma sida.
  </Card>
  <Card title="Aktivitetslogg" icon="scroll" href="/guider/settings-audit-log">
    Samma slags händelser, men för människor i stället för system.
  </Card>
</CardGroup>
